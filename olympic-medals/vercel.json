{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run clean && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --force",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
} 